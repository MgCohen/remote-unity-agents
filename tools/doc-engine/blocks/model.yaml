type: model
description: The domain shape — type sketches, interface surfaces, or a diagram; fenced code and ASCII diagrams are welcome here.
rubric:
  shape-not-prose:   Shows the shape (types, interfaces, a diagram) — no prose restating what the sketch already says.
  elements-anchored: Every element the model names appears elsewhere in the doc.
body:
  type: markdown
